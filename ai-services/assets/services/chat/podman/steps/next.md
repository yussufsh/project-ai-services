- Access the Q&A Chatbot web interface at http://{{ .HOST_IP }}:{{ .UI_PORT }}.

- Use the Q&A API for programmatic access at http://{{ .HOST_IP }}:{{ .BACKEND_PORT }}.

- Run "ai-services application info {{ .AppName }} --runtime podman" to view service endpoints.