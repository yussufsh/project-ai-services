pods:
  - name: "{{ .AppName }}--chat"
    format: "index .Ports \"3000/tcp\" 0"
    default: ""
    alias: UI_PORT

  - name: "{{ .AppName }}--chat"
    format: "index .Ports \"5000/tcp\" 0"
    default: ""
    alias: BACKEND_PORT

containers:
  - name: "{{ .AppName }}--chat-ui"
    format: ".Status"
    alias: UI_STATUS

  - name: "{{ .AppName }}--chat-backend-server"
    format: ".Status"
    alias: BACKEND_STATUS

hosts:
  - fetch: HOST_IP
    type: ip

# Made with Bob
