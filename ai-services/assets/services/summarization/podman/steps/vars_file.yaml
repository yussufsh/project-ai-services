pods:
  - name: "{{ .AppName }}--summarization"
    format: "index .Ports \"6000/tcp\" 0"
    default: ""
    alias: SUMMARIZE_API_PORT

containers:
  - name: "{{ .AppName }}--summarization-summarize-api"
    format: ".Status"
    alias: SUMMARIZE_API_STATUS

hosts:
  - fetch: HOST_IP
    type: ip

# Made with Bob
